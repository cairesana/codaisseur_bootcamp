//use a while loop to build it
//create a program that starts counting at 5;
// it counts up to 50 (including);
// stop condition: has counted to 50;
//count every fifth number


var count = 0;

while (count < 50) {
  count = count + 5;
  console.log(count);
};
